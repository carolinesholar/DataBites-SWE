# DataBites-SWE
how to run backend:

1. cd Backend
2. py -m pip install flask flask-cors werkzeug
3. py init_db.py   # only run once to create tables
4. py app.py

backend runs at http://127.0.0.1:5000
